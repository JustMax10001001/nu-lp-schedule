<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SchedulerTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.SchedulerTheme.ClassCard" parent="Theme.SchedulerTheme">
        <item name="cardElevation">@dimen/card_elevation_normal</item>
        <item name="cornerRadius">@dimen/card_corner_radius</item>
        <item name="android:stateListAnimator">@animator/card_state_list_animator</item>
    </style>

    <style name="Theme.SchedulerTheme.ClassCard.PrimaryInfo" parent="Theme.SchedulerTheme">
        <item name="android:background">?colorPrimary</item>
        <item name="android:textColor">?colorOnPrimary</item>
    </style>

    <style name="Theme.SchedulerTheme.ClassCard.GroupName" parent="Theme.SchedulerTheme.ClassCard.PrimaryInfo">
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textAppearance">?attr/textAppearanceHeadline5</item>
        <item name="android:textSize">16sp</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
    </style>

    <style name="Theme.SchedulerTheme.AlertDialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert" />

    <style name="Theme.SchedulerTheme.Widget.PopupMenu" parent="Widget.AppCompat.PopupMenu" />

    <style name="Theme.SchedulerTheme.NoActionBar" parent="Theme.SchedulerTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SchedulerTheme.NumeratorSwitch" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>

    <style name="Theme.SchedulerTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
    </style>

    <style name="Theme.SchedulerTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>